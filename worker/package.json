{
	"name": "sj-wao-metrics-worker",
	"version": "1.0.0",
	"description": "Metrics ingestion Worker + KV for the sj-wao dashboard",
	"private": true,
	"type": "module",
	"scripts": {
		"test": "vitest run",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.18.0",
		"@cloudflare/workers-types": "^4.20260702.1",
		"typescript": "^6.0.3",
		"vitest": "^4.1.9",
		"wrangler": "^4.107.0"
	}
}
